const express = require('express');
const router = express.Router();

const portfoilioControllers = require('../controllers/portfolioController');

// router.route('/:userId').get(portfoilioControllers.renderHome);
